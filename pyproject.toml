[project]
name = "proyecto-final"
version = "0.1.0"
description = ""
authors = [
    {name = "CrisbelFerrufino", email = "202505846@est.umss.edu"}
]
readme = "README.md"
requires-python = ">=3.11,<3.15"

[tool.poetry]
packages = [{ include = "programas", from = "src" }]

[tool.poetry.dependencies]
python = ">=3.11,<3.15"
matplotlib = ">=3.10.7,<4.0.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.0.2,<10.0.0)"
]
