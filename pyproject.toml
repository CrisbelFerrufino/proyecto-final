[tool.poetry]
name = "proyecto-final"
version = "0.1.0"
description = ""
authors = ["CrisbelFerrufino <202505846@est.umss.edu>"]
readme = "README.md"

packages = [
    { include = "proyecto_final", from = "src" }
]

[tool.poetry.dependencies]
python = ">=3.11,<3.15"
matplotlib = ">=3.10.7,<4.0.0"
pytest = ">=8.4.2,<9.0.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
